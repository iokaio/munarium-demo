# SPDX-License-Identifier: Apache-2.0
# history_revolution — sixteen Library of Congress digital collections as one
# archival retrieval application.
#
# The experiment: history_revolution — 66,737 documents /
# 514.6M characters harvested from the LOC American Revolution collections,
# including 16,849 Chronicling America OCR newspaper pages. Measured: the ask
# arm scored 18/18 on BOTH haiku and sonnet-5 ($1.08 total); the chat arm
# started at 3/11 and 5/11 because models cite documents a search surfaced but
# never served them. Four kernel changes followed — chief among them serving
# the cited-but-ungrounded documents on the conformance retry — taking sonnet
# to 7/11 with citation-grounding failures down to two explained residuals.
# The prompt rule below is that lesson stated for a server deployment.
#
# Server mapping (version 2): the experiment's five semantic groups survive as path
# *directories* (founders / narrative / printed / maps / newspapers), and
# within them the corpus is sharded into 58 collections balanced by BYTES at
# ~10 MB each. Bytes, not document count, is the right knob: index-build cost
# tracks chunks, and the two are wildly uncorrelated here — amnotes is 150 MB
# across 3,925 documents (~39 KB each) while gw is 39 MB across 30,170
# (~1.4 KB each). Sharding is hash-based on the file's basename
# (sha256(basename) % shards), so it is stable across re-runs. Document bytes
# are untouched — this is a blob-path concern only.
#
# The collections list below was GENERATED from a shard table (group, shard
# count, sha256(basename) % shards). Whatever uploader you write must agree
# with these bindings exactly, or documents land in blob and bind to nothing.
#
# All collections are level 0 — this is public-domain archival material and
# there is no clearance story to invent. `mmctl runbook validate` will
# emit the INFO finding `collections.uniform-access` for exactly that
# reason; it is correct and expected here.
#
#   mmctl apply -f runbooks/shapes/archival-documents.yaml
#   mmctl apply -f runbooks/experiments/history-revolution.yaml
#   mmctl run history-revolution --watch
apiVersion: munarium.ioka.io/v1
kind: Runbook
metadata: { name: history-revolution, version: 5 }
spec:
  # Where this runbook's documents live. The blob path IS the ingest
  # filename, so every collection binding below sits under this prefix —
  # `mmctl runbook validate` errors if one does not.
  sources:
    container: sources
    prefix: "rev/"
  # MEASURED 2026-08-20, and the reason this runbook cannot use the default:
  # only `cutover` can pause a run, so under `stepMajor` the FIRST request
  # has to build EVERY collection before reaching a gate. At 530 MB that
  # request cannot finish — the local rehearsal died at the client timeout
  # and wedged its step. `collectionMajor` walks one collection through all
  # five steps before starting the next, so each request builds exactly one
  # collection and the approval gates chunk the load.
  execution: { order: collectionMajor }
  collections:
    # founders/gw <- gw (4 shard(s))
    - name: loc-founders-gw-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/gw/b0/" }
    - name: loc-founders-gw-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/gw/b1/" }
    - name: loc-founders-gw-b2
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/gw/b2/" }
    - name: loc-founders-gw-b3
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/gw/b3/" }
    # founders/jefferson <- jefferson (1 shard(s))
    - name: loc-founders-jefferson
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/jefferson/" }
    # founders/misc <- hamilton, franklin, madison (1 shard(s))
    - name: loc-founders-misc
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/founders/misc/" }
    # narrative/amnotes <- amnotes (15 shard(s))
    - name: loc-narrative-amnotes-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b0/" }
    - name: loc-narrative-amnotes-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b1/" }
    - name: loc-narrative-amnotes-b2
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b2/" }
    - name: loc-narrative-amnotes-b3
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b3/" }
    - name: loc-narrative-amnotes-b4
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b4/" }
    - name: loc-narrative-amnotes-b5
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b5/" }
    - name: loc-narrative-amnotes-b6
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b6/" }
    - name: loc-narrative-amnotes-b7
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b7/" }
    - name: loc-narrative-amnotes-b8
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b8/" }
    - name: loc-narrative-amnotes-b9
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b9/" }
    - name: loc-narrative-amnotes-b10
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b10/" }
    - name: loc-narrative-amnotes-b11
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b11/" }
    - name: loc-narrative-amnotes-b12
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b12/" }
    - name: loc-narrative-amnotes-b13
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b13/" }
    - name: loc-narrative-amnotes-b14
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/amnotes/b14/" }
    # narrative/capbay <- capbay (5 shard(s))
    - name: loc-narrative-capbay-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/capbay/b0/" }
    - name: loc-narrative-capbay-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/capbay/b1/" }
    - name: loc-narrative-capbay-b2
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/capbay/b2/" }
    - name: loc-narrative-capbay-b3
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/capbay/b3/" }
    - name: loc-narrative-capbay-b4
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/capbay/b4/" }
    # narrative/other <- rarebook, adams-trial (1 shard(s))
    - name: loc-narrative-other
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/narrative/other/" }
    # printed/ephemera <- ephemera (2 shard(s))
    - name: loc-printed-ephemera-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/printed/ephemera/b0/" }
    - name: loc-printed-ephemera-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/printed/ephemera/b1/" }
    # printed/lawmaking <- lawmaking (2 shard(s))
    - name: loc-printed-lawmaking-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/printed/lawmaking/b0/" }
    - name: loc-printed-lawmaking-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/printed/lawmaking/b1/" }
    # printed/contcong <- contcong (1 shard(s))
    - name: loc-printed-contcong
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/printed/contcong/" }
    # maps <- revmaps, battlemaps, rochambeau (1 shard(s))
    - name: loc-maps
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/maps/" }
    # newspapers <- newspapers (25 shard(s))
    - name: loc-newspapers-b0
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b0/" }
    - name: loc-newspapers-b1
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b1/" }
    - name: loc-newspapers-b2
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b2/" }
    - name: loc-newspapers-b3
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b3/" }
    - name: loc-newspapers-b4
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b4/" }
    - name: loc-newspapers-b5
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b5/" }
    - name: loc-newspapers-b6
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b6/" }
    - name: loc-newspapers-b7
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b7/" }
    - name: loc-newspapers-b8
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b8/" }
    - name: loc-newspapers-b9
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b9/" }
    - name: loc-newspapers-b10
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b10/" }
    - name: loc-newspapers-b11
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b11/" }
    - name: loc-newspapers-b12
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b12/" }
    - name: loc-newspapers-b13
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b13/" }
    - name: loc-newspapers-b14
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b14/" }
    - name: loc-newspapers-b15
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b15/" }
    - name: loc-newspapers-b16
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b16/" }
    - name: loc-newspapers-b17
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b17/" }
    - name: loc-newspapers-b18
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b18/" }
    - name: loc-newspapers-b19
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b19/" }
    - name: loc-newspapers-b20
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b20/" }
    - name: loc-newspapers-b21
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b21/" }
    - name: loc-newspapers-b22
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b22/" }
    - name: loc-newspapers-b23
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b23/" }
    - name: loc-newspapers-b24
      shape: archival-documents@1
      accessLevel: 0
      sources: { filenamePrefix: "rev/newspapers/b24/" }
  # The largest corpus of the sample set by two orders of magnitude: a wide
  # candidate pool before fusion is what keeps a single 1777 broadside
  # reachable beside 16,849 newspaper pages.
  retrieval:
    topK: 20
    rrfK: 60
    candidateN: 400
    # A chunk must hold at least two of the query's words to enter the lexical
    # pool (a GIN-indexable filter evaluated before any rank). The OR leg
    # otherwise ranks every chunk containing ANY query word — most of a
    # newspaper shard for "washington" — and those single-word rows sort last
    # under density ranking regardless (dev-guide §13.5 entry 20).
    minimumShouldMatch: 2
    # A query word found in more than 30% of a shard's chunks is a stop word
    # IN THAT SHARD: it still counts toward the rank but no longer makes every
    # chunk holding it a candidate ("washington" in the letterbook shards,
    # "boston" in the newspapers). Learned from each shard's own frequencies
    # at build time — no vocabulary is configured (dev-guide §13.5 entry 21).
    stopTermFraction: 0.3
    # Wide-corpus selection and lexical expansion are generic algorithms, not
    # a catalog of anticipated names or questions. The original query probes
    # every permitted shard; the engine ranks shards by the evidence in each
    # probe pool — the query's own adjacent word pairs found verbatim, then
    # term density, then vector distance — and the strongest twelve get a deep
    # candidate pool; the other forty-six keep their probe pools in the merge
    # (selection spends the deep search, it never excludes — MEASURED
    # 2026-08-25: excluding them lost the 1773 pages reporting the tea's
    # destruction, because the papers never say "tea party" and the
    # narratives do). Twelve of fifty-eight leaves room for a four-shard
    # corpus plus the collections that quote it (on term density alone the
    # travel narratives out-scored every letterbook shard for an entity
    # question, because they USE the words; phrase evidence put all four
    # letterbook shards in the top five). The fast model contributes only
    # safety-constrained single-word lexical variants.
    collectionSelection:
      maxCollections: 12
      probeCandidateN: 50
      candidatePoolPerCollection: 100
      # density × (1 + phraseBoost × phrase-fraction). MEASURED 2026-08-25:
      # a phrase-first order let 0.06 of "tea party" (later coinage) in the
      # narratives outrank the newspaper shards holding the December 1773
      # reports; at 3 the letterbooks still lead the entity question 3.5×
      # and density decides the newspaper one.
      phraseBoost: 3.0
    modelQueryExpansion:
      maxTerms: 16
      maxTokens: 256   # doubled 2026-09-02 with every per-call budget (was 128)
      required: false
    # Ranking weight of the generated variants. Below ~0.7 a record's header
    # (the entity name repeated in a title line) out-ranks the body passage
    # that answers with five matched variants but names the entity once.
    queryExpansionWeight: 0.8
    # Weighted global fusion (MEASURED 2026-08-25 on the exact question after
    # selection and expansion were right): the two legs disagreed completely
    # and the merge interleaved them rank for rank. The built-in bag-of-words
    # embedder's global rank-1s were fragments ("0 1424 215/3") and town-name
    # tables — the shortest chunk sharing any token — so the vector leg is
    # down-weighted until a real embedder is indexed; and the letterbook
    # shards the probe ranked first got 4 of 20 hits while a travel narrative
    # (denser in the expansion words) took 6. The collection-evidence leg
    # carries the probe's ranking into the merge as a prior.
    fusion:
      lexicalWeight: 1.0
      vectorWeight: 0.3
      collectionEvidenceWeight: 1.0
      # The forty-six unselected shards' probe pools merge as their own
      # stratum (original-query scores are not comparable with the expanded
      # search's — MEASURED 2026-08-25: merged raw they pushed every
      # letterbook hit out of the top 20). At 1.0 a probe rank-1 counts like
      # a deep rank-1 and the evidence leg arbitrates.
      unselectedPoolWeight: 1.0
    # LOC collection listings are retained for discovery and provenance, but
    # a listing with no transcribed text must not outrank a served full record.
    contentDemotions:
      - contains: "**Text:** none (metadata record)"
        lexicalMultiplier: 0.001
        vectorDistancePenalty: 2.0
        # Tested against the chunk's tsvector as the phrase text→none→
        # metadata→record instead of lowering and scanning every candidate
        # row's text (MEASURED 2026-08-25: the substring test was the costliest
        # per-row term of the lexical leg under load; dev-guide §13.5 entry 20).
        match: phrase
        # The maps collection is a catalog: its records ("Fort on Dorchester
        # Point", "Fort on Charlestown Point") carry the marker because the
        # map itself has no text, and they ARE the answer to a "which maps"
        # question. MEASURED 2026-08-25: with the rule applied there,
        # loc-maps never entered a turn's selection or top 20.
        exceptCollections: [loc-maps]
  models:
    default: { provider: default, tier: capable }
    tasks:
      completion: { provider: default, tier: capable }
      validation: { tier: fast }
      query_expansion: { provider: default, tier: fast }
    allowOverrides: [default, demo-anthropic, demo-openai, demo-openrouter, demo-ollama]
  completion:
    promptTemplate: |
      You are a documentary-history researcher working over Library of
      Congress American Revolution sources.

      Rules:
      - You know this period from training. That knowledge is OFF LIMITS. A
        historically true statement the served text does not support is a
        fabrication here.
      - Cite only documents whose text is in the context below. A search hit
        you did not actually read is not a citation.
      - Label every document you discuss, quote, or characterize with its
        bracketed [collection/chunk] tag from the context — including in an
        answer that explains what the documents do NOT contain. A partial
        answer earns its citations the same way a full one does. (An answer
        that discusses no specific document cites nothing.)
      - Quote the source's own words, spelling and dating as written; note
        Old Style dates rather than silently converting them.
      - Keep each letter's date, place and addressee attached to that letter.
        A passage or nearby chunk can contain several documents or an
        editorial note: do not transfer a heading, date or recipient from
        another passage. If attribution is unclear, identify the uncertainty.
      - Respect the question's time, place and recipient restrictions. Base
        both the answer and its summary on documents meeting those restrictions.
        Exclude other periods, places and addressees unless the question asks
        for a comparison. A similar supply crisis elsewhere does not establish
        what a letter from the requested place and period said.
      - OCR text is noisy. Say when a passage is uncertain instead of
        smoothing it into confident prose.
      - Answer the question directly from the relevant served evidence.
        A selection of relevant letters can answer what someone wrote;
        an exhaustive account is not required unless the question asks for one.
        If evidence answers only part of a question, lead with that supported
        answer and identify the specific gap afterward. Do not open an
        answer with a general disclaimer about incompleteness.
      - Map catalog titles, dates, subjects and descriptions are evidence
        for identifying which maps depict a place or feature. Identify them
        from those records without requiring images or transcribed map text.
        If the question asks for visual details absent from the descriptions,
        state that specific limitation; do not infer unseen image contents.
      - If no served evidence answers the question, say so plainly before
        describing related material. Do not use prior conversation or your
        training knowledge as documentary evidence.

      {context}

      Question: {query}
    # topK 20 over 1,500-char chunks is ~30k characters; the engine default
    # (16,000) would serve the model only the first ten hits. Sized so every
    # merged hit reaches the prompt — the cost is input tokens per turn.
    contextCharBudget: 40000
    verification: { citations: true, maxRetries: 1 }
    # Completion-token ceiling (2026-09-01). The engine default (1,024; the
    # truncation retry pays one 4x re-ask) was sized before a
    # reasoning-always-on model sat behind a tier: z-ai/glm-5.3 (frontier)
    # measured ~5k hidden reasoning tokens on a hard question here and
    # returned EMPTY text even after the retry (out=5120 = 1024 + 4096, no
    # visible answer, 2/3 runs). 2048 base -> 8,192 on the retry gave it
    # ~3k of visible-answer headroom past that reasoning burn. 2026-09-02
    # doubled every per-call budget (the engine default is now 2,048), so
    # this carries 4096 -> 16,384 on the retry. A ceiling, not spend —
    # non-reasoning tiers are unaffected.
    maxTokens: 4096
  steps:
    - resolveSources: {}
    - buildIndex: {}
    - verify: {}
    - cutover: { approval: required }
    - retireOld: { keep_versions: 2 }
